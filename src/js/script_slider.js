$(document).ready(function () {
  $('.slider').slick({
    arrows: false,
    dots: true,
    adaptiveHeight: true,
    speed: 500,
    easing: 'linear',
    autoplay: true,
    autoplaySpeed: 5000,
  });
});
